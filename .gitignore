node_modules/
dist/
.turbo/
.astro/
coverage/
*.tsbuildinfo

# local publish state (slug -> record rkey); real sites commit their own
.publish-state.json

# generated by apps/demo/e2e/serve.mjs each run (local-net env for the specs)
apps/demo/e2e/.local-net.json

# Playwright
playwright-report/
test-results/
blob-report/

# secrets / local env — never commit app passwords
.env
.env.*
!.env.example

.DS_Store
